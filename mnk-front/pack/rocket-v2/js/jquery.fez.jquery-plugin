(function($)
{
    $.fn.[PLUGIN_NAME]=function(paramSend)
    {

      var defauts=
           {  
            param   : "", 
           };  


      var param=$.extend(defauts, paramSend);

      $(document).on("click",this.selector , function (){
         
      };
})(jQuery);